# Architecture Decision Records

Short notes on decisions that were not obvious, written at the time the
decision was made. Each one records the alternatives that were rejected and
why, because "why not the simpler thing?" is the only question a reviewer
actually asks.
