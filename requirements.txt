fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0
redis==5.2.1
aiokafka==0.12.0
# Standard client for the Prometheus exposition format — Counter/Histogram/Gauge
# types, a registry, and /metrics text serialization. Nothing in the stdlib
# or an existing dependency covers that wire format; hand-rolling it would
# be redundant with the one library the whole scrape ecosystem expects.
prometheus-client==0.21.1
