-r requirements.txt
pytest==8.3.4
pytest-asyncio==0.25.0
ruff==0.8.4
# ASGI-aware async client. Needed so integration tests can drive the FastAPI
# app over real HTTP semantics (status codes, headers) instead of calling
# service functions directly — the two bugs this suite targets were bugs in
# routes.py's status-code mapping, which a service-layer call would skip
# straight past. The stdlib has no ASGI transport; TestClient itself is
# httpx-based as of Starlette >=0.37.
httpx==0.28.1
